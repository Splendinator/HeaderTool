#include "CodeParseTokenStructMetadata.h"

void CodeParseTokenStructMetadata::WriteToFile(std::ofstream& outputFile)
{
	// I haven't thought of any uses for struct metadata yet.
	// This code parse token was just needed because classes use metadata and we treat structs/classes the same.
}
